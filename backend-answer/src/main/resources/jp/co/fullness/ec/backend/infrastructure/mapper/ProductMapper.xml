<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.fullness.ec.backend.infrastructure.mapper.ProductMapper">

    <!-- 商品検索(論理削除除外・カテゴリ絞り込み・ID昇順・ページング) -->
    <select id="search" resultType="ProductEntity">
        SELECT id, name, price, image_url, product_category_id, delete_flg
        FROM product
        WHERE delete_flg = 0
        <if test="categoryId != null">
            AND product_category_id = #{categoryId}
        </if>
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 検索条件に一致する件数 -->
    <select id="count" resultType="long">
        SELECT COUNT(*)
        FROM product
        WHERE delete_flg = 0
        <if test="categoryId != null">
            AND product_category_id = #{categoryId}
        </if>
    </select>

    <insert id="insert" parameterType="ProductEntity"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, price, image_url, product_category_id, delete_flg)
        VALUES (#{name}, #{price}, #{imageUrl}, #{productCategoryId}, #{deleteFlg})
    </insert>

    <select id="selectById" resultType="ProductEntity">
        SELECT id, name, price, image_url, product_category_id, delete_flg
        FROM product
        WHERE id = #{id}
    </select>

    <update id="update" parameterType="ProductEntity">
        UPDATE product
        SET name = #{name},
            price = #{price},
            image_url = #{imageUrl},
            product_category_id = #{productCategoryId}
        WHERE id = #{id}
    </update>

    <!-- 論理削除: 物理削除せず delete_flg を 1 に更新 -->
    <update id="logicalDelete" parameterType="int">
        UPDATE product
           SET delete_flg = 1
         WHERE id = #{id}
    </update>
    
</mapper>