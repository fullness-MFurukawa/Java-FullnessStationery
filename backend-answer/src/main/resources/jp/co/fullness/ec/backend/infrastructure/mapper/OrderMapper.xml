<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.fullness.ec.backend.infrastructure.mapper.OrderMapper">

    <!-- 条件付き検索(注文ID降順・ページング) -->
    <select id="search" resultType="OrderEntity">
        SELECT id, order_date, amount_total, customer_id, order_status_id, payment_method_id
          FROM orders
        <where>
            <if test="condition.orderDate != null">
                AND DATE(order_date) = #{condition.orderDate}
            </if>
            <if test="condition.customerId != null">
                AND customer_id = #{condition.customerId}
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 件数(ページング用) -->
    <select id="count" resultType="long">
        SELECT COUNT(*)
          FROM orders
        <where>
            <if test="condition.orderDate != null">
                AND DATE(order_date) = #{condition.orderDate}
            </if>
            <if test="condition.customerId != null">
                AND customer_id = #{condition.customerId}
            </if>
        </where>
    </select>

    <!-- 1件取得 -->
    <select id="selectById" resultType="OrderEntity" parameterType="int">
        SELECT id, order_date, amount_total, customer_id, order_status_id, payment_method_id
          FROM orders
         WHERE id = #{id}
    </select>

    <!-- ステータス更新(UC016) -->
    <update id="updateStatus">
        UPDATE orders
           SET order_status_id = #{newStatusId}
         WHERE id = #{orderId}
    </update>

    <!-- 購入日プルダウン用: 注文のある日付を新しい順に -->
    <select id="selectDistinctOrderDates" resultType="java.time.LocalDate">
        SELECT DISTINCT DATE(order_date) AS order_date
          FROM orders
         ORDER BY DATE(order_date) DESC
    </select>

</mapper>