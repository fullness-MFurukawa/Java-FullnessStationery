<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.fullness.ec.frontend.infrastructure.mapper.ProductMapper">

    <select id="searchByCategory" resultType="ProductEntity">
        SELECT id, name, price, image_url, product_category_id, delete_flg
          FROM product
        <where>
            delete_flg = 0
            <if test="categoryId != null">
                AND product_category_id = #{categoryId}
            </if>
        </where>
        ORDER BY id
    </select>

    <select id="selectById" resultType="ProductEntity" parameterType="int">
        SELECT id, name, price, image_url, product_category_id, delete_flg
          FROM product
         WHERE id = #{id}
    </select>

</mapper>