<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="jp.co.fullness.ec.frontend.infrastructure.mapper.ProductStockMapper">

    <select id="selectByProductId" resultType="ProductStockEntity" parameterType="int">
        SELECT id, quantity, product_id FROM product_stock WHERE product_id = #{productId}
    </select>

    <select id="lockByProductId" resultType="ProductStockEntity" parameterType="int">
        SELECT id, quantity, product_id FROM product_stock WHERE product_id = #{productId} FOR UPDATE
    </select>

    <update id="decreaseQuantity">
        UPDATE product_stock
           SET quantity = quantity - #{amount}
         WHERE product_id = #{productId}
    </update>

</mapper>